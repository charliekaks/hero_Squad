
#if ($heros)
    #foreach ($hero in $heros)
    <div class="well">
      <p>$hero.getName()</p>
      <p>$hero.getAge()</p>
      <p>$hero.getSuperPower()</p>
      <p>$hero.getWeakness()</p>
      <li><a href="/hero/$hero.getId()">$hero.getName()</a></li>
    </div>
    #end
    
#else
    <p>No heroes!</p>
#end
<div class="well">
    <a href="/heroes/new">Add New Hero</a>
</div>
